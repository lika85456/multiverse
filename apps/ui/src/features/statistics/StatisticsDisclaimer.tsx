import * as React from "react";

export default function StatisticsDisclaimer() {
    return <div className="text-sm font-thin text-tertiary my-2">
        * statistics are periodically updated, the latest data may not be available yet
    </div>;
}